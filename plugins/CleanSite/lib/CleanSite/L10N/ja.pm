package CleanSite::L10N::ja;
use strict;
use warnings;
use base qw( CleanSite::L10N );

our %Lexicon
    = ( 'Remove gabages, keep sites clean.' =>
        '管理画面上で削除されたデータのファイルをきちんと削除します。',
    );

1;
